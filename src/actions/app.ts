// import { createActions } from 'redux-actions';

// export const { increment, decrement } = createActions({
//   INCREMENT: (amount: number) => ({ amount }),
//   DECREMENT: (amount: number) => ({ amount: -amount })
// });

export const handleGoBack = () => ({ type: 'HANDLE_GO_BACK' });
